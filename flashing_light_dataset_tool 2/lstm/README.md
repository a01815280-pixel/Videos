# Your LSTM

This project intentionally does not include an LSTM implementation or TensorFlow installation.

The dataset tool puts manually labeled videos here:

- `data/raw/flashing/`
- `data/raw/no_flashing/`

Place or connect your existing LSTM project here later, or modify `src/app.py` to call your existing LSTM.
